apiVersion: v1
kind: Service
metadata:
  name: inventory-api
  namespace: production
  labels:
    app: inventory-api
    team: backend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: inventory-api
